A1: ls -l > listing.txt; date >> listing.txt
A2: ls /nonexistent_dir 2> errors.txt; standard error is file descriptor 2
A3: ls /nonexistent_dir /etc > output.txt 2> errors.txt
A4: ls /nonexistent_dir /etc &> combined.txt
A5: ls /nonexistent_dir /etc 2>/dev/null; I only see the /etc output because the error is sent to /dev/null, which discards it.
A6: sort fruits.txt | uniq
A7: sort fruits.txt | uniq -c | sort -nr
A8: grep "ERROR" server.log | wc -l
A9: grep "ERROR" server.log | cat > errors_only.txt
A10: grep -oE '^[A-Z]+' server.log | sort | uniq -c | sort -nr
A11: grep -oE '"state":"[A-Z]{2}"' /data/yelp/yelp_academic_dataset_business.json | cut -d'"' -f4 | sort | uniq -c | sort -nr | head -5
B1: uid = 1052 user ID; gid = 1052 main group ID
B2: ls -l fruits.txt; permissions were -rw-rw-r--; owner can read/write, group can read/write, others can read only.
B3: umask showed 0002. For a new file, this produces permissions 664, or -rw-rw-r--, meaning owner can read/write, group can read/write, and others can read only. I verified it with echo "top secret" > secret.txt and ls -l secret.txt.
B4: chmod 000 secret.txt; cat secret.txt showed Permission denied; chmod u+rw secret.txt
B5: chmod 640 fruits.txt; octal value 640
B6: find . -type f -user "$(whoami)" -perm -200
C1: bash long.sh > log.txt &; PID was 2045821; tail log.txt
C2: [1]+  Running                 bash long.sh > log.txt &
C3: fg; Ctrl-Z; jobs showed the job state as Stopped.
C4: bg; jobs showed running
C5: kill %1; jobs; kill sends SIGTERM by default.
C6: bash long.sh > log.txt &; disown %1; jobs; ps -p 2046822
C7: disown lets the process keep running after I close the terminal because it is no longer connected to the shell’s job list
